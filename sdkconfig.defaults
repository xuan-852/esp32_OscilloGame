# 让 mbedTLS 使用 PSRAM 分配 SSL 缓冲区
CONFIG_MBEDTLS_EXTERNAL_MEM_ALLOC=y
# 加大 SSL 记录缓冲区到 32KB（默认 16KB）
CONFIG_MBEDTLS_SSL_MAX_CONTENT_LEN=32768
# 关闭证书保留（节省 ~16KB 堆）
CONFIG_MBEDTLS_SSL_KEEP_PEER_CERTIFICATE=n
